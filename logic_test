import math
import statistics


s1 = [1/2, 2/3]
s2 = [1/2, 2/3, 1/3]
s3 = [1/3, 1/3, 2/3]

s1_mean = statistics.mean(s1)
s2_mean = statistics.mean(s2)
s3_mean = statistics.mean(s3)

sum = s1_mean + s2_mean + s3_mean

print(sum)
print(s1_mean)
print(s2_mean)
print(s3_mean)

#spróbuj zrobić tak że szansa na obecność miny na polu to średnia wszystkich szans otrzymywanych od pola z liczbą
#potem przy klikaniu po prostu klikasz w to pole na całej planszy które ma najmniejszą szansę na minę, jeśli jest zero to na luzie
#jeśli najmniejsza szansa na minę na polu jest niezerowa to co wtedy? czy to się uda?

#jak się okazało prostymi testami to się nie uda, korelacja pól jest konieczna
#zatem wymyśliłem żeby zrobić tak że każda cyferka tworzy wszystkie konfiguracje w jakich są miny i pola bezpieczne na polach sąsiadujących
#następnie cyferki porównują swoje konfiguracje z konfiguracjami innych cyferek i usuwają konfiguracje które się wzajemnie wykluczają
#wykluczenie może sprawić że szansa na obecność miny na polu zmieni się na 0 lub 1
#więc tak, zostawiamy obliczanie szansy miny na polu, tylko teraz nigdy nie będziemy klikać w ułamki, obecność ułamków będzie trigerowała konfiguracje